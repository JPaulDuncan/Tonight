{
  "name": "tonight",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Tonight - a browser battle royale in three.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "smoke": "vite build && vite preview --port 4180 --strictPort & sleep 5 && node tools/smoke.mjs http://localhost:4180/ /tmp/tonight.png",
    "verify": "npm run typecheck && npm run test && npm run build",
    "art": "python3 ../blender/scripts/build_all.py --out public/art",
    "predev": "npm run art",
    "prebuild": "npm run art"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "@types/three": "^0.186.0",
    "playwright": "^1.63.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
